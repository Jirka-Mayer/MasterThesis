class DeepScores:
    """
    Represents the DeepScoresV2 dataset content
    in a more experiment-friendly way
    """
    
    # DPSS = dots (pixels) per staff space
    # (something like DPI, but in a more useful context)
    DPSS = 16

    TEST_PAGE_COUNT = 352
    TRAIN_PAGE_COUNT = 1362
